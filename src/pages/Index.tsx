import { BrowserRouter } from "react-router-dom";
import { MoneyMindApp } from "@/components/MoneyMindApp";

const Index = () => {
  return (
    <BrowserRouter>
      <MoneyMindApp />
    </BrowserRouter>
  );
};

export default Index;
